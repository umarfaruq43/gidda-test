import React, { createContext } from "react";

const createProperty = createContext();

const PropertyCreationContext = () => {
    return <div>PropertyCreationContext</div>;
};

export default PropertyCreationContext;
